
<p class="address">{{section.settings.map-address}}</p>
<div class="map-responsive">{{section.settings.map_embed}}</div>




{% schema %}

    {
        "name": "Embed",
        "settings": [
            {
                "type": "html",
                "id": "map_embed",
                "label": "Map Embed"
            },
            {
                "type": "text",
                "id": "map-address",
                "label": "Address"
            }
        ]
    }

{% endschema %}
